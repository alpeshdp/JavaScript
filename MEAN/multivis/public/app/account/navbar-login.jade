.navbar-right(ng-controller="NavBarLoginController")
    form.navbar-form(ng-hide="identitySvc.isAuthenticated()")
        .form-group
            input.form-control(placeholder="User Name", ng-model="userName")
        .form-group
            input.form-control(type="password", placeholder="Password", ng-model="password")
        button.btn.btn-primary(ng-click='signIn(userName, password)') SignIn